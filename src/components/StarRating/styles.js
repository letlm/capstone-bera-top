import styled from "styled-components";

export const Star = styled.div`
    input[type="radio"] {
        display: none;
    }

    .star {
        cursor: pointer;
        transition: color 200ms;
    }
`;
