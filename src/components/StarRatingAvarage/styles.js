import styled from "styled-components";

const Star = styled.div`
  input[type="radio"] {
    display: none;
  }

  .star {
    cursor: pointer;
    transition: color 200ms;
  }
`;
export default Star;
